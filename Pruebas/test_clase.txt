{
CLASS test_clase {
    INT property1,
    FLOAT property2,

    VOID setter(INT new_property) {
        property1 = new_property,
        RETURN,
        },

    clase_herencia,
    },

    test_clase c1;c2,

    c1.setter(1_i),
    a = c1.property1,
}